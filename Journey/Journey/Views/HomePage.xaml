<?xml version="1.0" encoding="utf-8"?>
<local:BasePage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:fftransformations="clr-namespace:FFImageLoading.Transformations;assembly=FFImageLoading.Transformations" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:local="clr-namespace:Journey.Views" xmlns:Controls="clr-namespace:Journey.Views.Controls" xmlns:resources="clr-namespace:Journey.Resources" xmlns:selectors="clr-namespace:Journey.Views.Selectors" x:Class="Journey.Views.HomePage">
    <ContentPage.Resources>
        <ResourceDictionary>
            <DataTemplate x:Key="ScaleTemplate">
                <StackLayout Orientation="Vertical">
                    <Label Text="{Binding Title}" HorizontalOptions="Center" />
                    <Controls:CustomImage Source="{Binding Image.Source}" Aspect="AspectFit" WidthRequest="50" HeightRequest="50" />
                    <StackLayout Orientation="Horizontal" HorizontalOptions="Center">
                        <Controls:CustomImage Source="{Binding Indictor.Source}" Aspect="AspectFit" WidthRequest="20" HeightRequest="20" />
                        <Label Text="{Binding MeasureWithUnit}" HorizontalOptions="Center" />
                    </StackLayout>
                </StackLayout>
            </DataTemplate>
            <DataTemplate x:Key="PostTemplate">
                <Controls:UnSelectableViewCell>
                    <ViewCell.View>
                        <Grid Margin="0,0,0,5" RowSpacing="0" BackgroundColor="White">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid Grid.Row="0" RowSpacing="0" Margin="10">
                                <Grid.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding OnProfileDetailsCommand}" />
                                </Grid.GestureRecognizers>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Controls:CustomImage Grid.Column="0" Grid.RowSpan="3" Source="{Binding Post.Account.Image.Path}" VerticalOptions="Start" HorizontalOptions="Start" WidthRequest="50" HeightRequest="70">
                                    <Controls:CustomImage.Transformations>
                                        <fftransformations:CircleTransformation />
                                    </Controls:CustomImage.Transformations>
                                </Controls:CustomImage>
                                <Label Grid.Column="1" Grid.Row="0" Text="{Binding Post.Account.Name}" TextColor="Black" FontSize="14" FontAttributes="Bold" VerticalOptions="Center" HorizontalOptions="Start" />
                                <Controls:ImageControl Grid.Column="1" Grid.Row="0" PortablePath="more.png" HorizontalOptions="End" Style="{StaticResource IconStyle}">
                                    <Image.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding OnPostDeleteCommand}" />
                                    </Image.GestureRecognizers>
                                </Controls:ImageControl>
                                <Label Grid.Column="1" Grid.Row="1" Text="{Binding Post.FormatedDate}" TextColor="Black" FontSize="14" VerticalOptions="Center" HorizontalOptions="End" />
                                <StackLayout Grid.Row="1" Grid.Column="1" Orientation="Horizontal" IsVisible="{Binding Post.HasActivity}" VerticalOptions="Center">
                                    <Label Text="{Binding Post.Activity.Action}" TextColor="Black" FontSize="12" HorizontalOptions="Start" VerticalOptions="Center" />
                                    <Controls:CustomImage Source="{Binding Post.Activity.Image.Path}" VerticalOptions="Center" HorizontalOptions="Start" WidthRequest="25" HeightRequest="25" />
                                    <Label Text="{Binding Post.Activity.Activity}" FontAttributes="Bold" TextColor="Black" FontSize="14" HorizontalOptions="Start" VerticalOptions="Center" LineBreakMode="NoWrap" />
                                </StackLayout>
                                <StackLayout Grid.Row="2" Grid.Column="1" Orientation="Horizontal" IsVisible="{Binding Post.HasLocation}" VerticalOptions="Center">
                                    <Label Text="{Binding Post.Location.Action}" TextColor="Black" FontSize="12" HorizontalOptions="Start" VerticalOptions="Center" />
                                    <Controls:CustomImage Source="{Binding Post.Location.Image.Path}" VerticalOptions="Center" HorizontalOptions="Start" WidthRequest="25" HeightRequest="25" />
                                    <Label Text="{Binding Post.Location.Activity}" FontAttributes="Bold" TextColor="Black" FontSize="14" HorizontalOptions="Start" VerticalOptions="Center" LineBreakMode="NoWrap" />
                                </StackLayout>
                                <Label Grid.Column="1" Grid.Row="0" Text="{Binding Post.PostType.FormatedExpPoint}" TextColor="Orange" FontSize="14" VerticalOptions="Start" HorizontalOptions="End" />
                            </Grid>
                            <Label Grid.Row="1" Text="{Binding Post.Feed}" TextColor="Black" Margin="5,10,10,5" FontSize="14" HorizontalOptions="Fill" />
                            <Controls:ImageViewerControl Grid.Row="2" IsVisible="{Binding Post.HasMeasuremnts}" BackgroundColor="White" ItemsSource="{Binding Post.Measuremnts}" Margin="0,10" VerticalOptions="StartAndExpand" HorizontalOptions="FillAndExpand" ItemTemplate="{StaticResource ScaleTemplate}" />
                            <Controls:ImageViewerControl Grid.Row="3" x:Name="parent" ItemsSource="{Binding Post.MediaList}" IsVisible="{Binding Post.HasMediaList}" HeightRequest="100" VerticalOptions="Center" HorizontalOptions="FillAndExpand">
                                <Controls:ImageViewerControl.ItemTemplate>
                                    <DataTemplate>
                                        <Controls:CustomImage Source="{Binding Source}">
                                            <Controls:CustomImage.GestureRecognizers>
                                                <TapGestureRecognizer Command="{Binding Path=BindingContext.OnGalleryDetailsCommand, Source={x:Reference Name=parent}}" />
                                            </Controls:CustomImage.GestureRecognizers>
                                        </Controls:CustomImage>
                                    </DataTemplate>
                                </Controls:ImageViewerControl.ItemTemplate>
                            </Controls:ImageViewerControl>
                            <Grid Grid.Row="4" Margin="0,5,0,5" VerticalOptions="End">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <StackLayout Grid.Column="0" Orientation="Horizontal" Margin="20,0,0,0" HorizontalOptions="Center">
                                    <Label Text="{Binding Post.LikesCount}" TextColor="Black" FontSize="14" VerticalOptions="Start" HorizontalOptions="End" />
                                    <Controls:ImageControl PortablePath="fav.png" Style="{StaticResource IconStyle}" IsVisible="{Binding Post.NotLiked}">
                                        <Image.GestureRecognizers>
                                            <TapGestureRecognizer Command="{Binding OnPostLikeCommand}" />
                                        </Image.GestureRecognizers>
                                    </Controls:ImageControl>
                                    <Controls:ImageControl PortablePath="unfav.png" Style="{StaticResource IconStyle}" IsVisible="{Binding Post.Liked}">
                                        <Image.GestureRecognizers>
                                            <TapGestureRecognizer Command="{Binding OnPostLikeCommand}" />
                                        </Image.GestureRecognizers>
                                    </Controls:ImageControl>
                                </StackLayout>
                                <StackLayout Grid.Column="1" Orientation="Horizontal" HorizontalOptions="Center">
                                    <Label Text="{Binding Post.CommentsCount}" TextColor="Black" FontSize="14" VerticalOptions="Start" HorizontalOptions="End" />
                                    <Controls:ImageControl PortablePath="comment.png" Style="{StaticResource IconStyle}">
                                        <Image.GestureRecognizers>
                                            <TapGestureRecognizer Command="{Binding OnPostCommentCommand}" />
                                        </Image.GestureRecognizers>
                                    </Controls:ImageControl>
                                </StackLayout>
                                <StackLayout Grid.Column="2" Orientation="Horizontal" HorizontalOptions="Center">
                                    <Label Text="{Binding Post.SharesCount}" TextColor="Black" FontSize="14" VerticalOptions="Start" HorizontalOptions="End" />
                                    <Controls:ImageControl PortablePath="share.png" Style="{StaticResource IconStyle}">
                                        <Image.GestureRecognizers>
                                            <TapGestureRecognizer Command="{Binding OnPostShareCommand}" />
                                        </Image.GestureRecognizers>
                                    </Controls:ImageControl>
                                </StackLayout>
                            </Grid>
                        </Grid>
                    </ViewCell.View>
                </Controls:UnSelectableViewCell>
            </DataTemplate>
            <DataTemplate x:Key="AdsTemplate">
                <Controls:UnSelectableViewCell>
                    <ViewCell.View>
                        <Grid RowSpacing="0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Controls:CustomImage HeightRequest="250" Margin="0,0,0,5" Source="{Binding Post.Image.Source}" VerticalOptions="Fill" HorizontalOptions="Fill" Aspect="AspectFill" />
                            <Button Grid.Row="1" Margin="5,0,5,5" Text="View More" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding OpenDeepLinkCommand}" />
                        </Grid>
                    </ViewCell.View>
                </Controls:UnSelectableViewCell>
            </DataTemplate>
            <selectors:PostTemplateSelector x:Key="PostTemplateSelector" PostTemplate="{StaticResource PostTemplate}" AdsTemplate="{StaticResource AdsTemplate}" />
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.Content>
        <Grid RowSpacing="0">
            <Grid.Margin>
                <OnPlatform x:TypeArguments="Thickness">
                    <On Platform="iOS" Value="0,25,0,0" />
                    <On Platform="Android, WinPhone, Windows" Value="0,0,0,0" />
                </OnPlatform>
            </Grid.Margin>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" BackgroundColor="White" HeightRequest="75">
                <SearchBar Margin="0,5,0,0" HorizontalOptions="StartAndExpand" BackgroundColor="White" VerticalOptions="Center" WidthRequest="400" Focused="Handle_Focused" Placeholder="Search for friends" Text="{Binding SearchKeyword,Mode=TwoWay}" SearchCommand="{Binding OnSearchCommand}" />
                <Controls:LoadingControl IsVisible="{Binding IsLoading}" HorizontalOptions="End" VerticalOptions="Center" Margin="20,0" />
                <Controls:ImageControl x:Name="img" HorizontalOptions="End" IsVisible="{Binding IsNotLoading}" VerticalOptions="Center" Margin="20,0" PortablePath="loading.png" Style="{StaticResource IconStyle}" />
            </Grid>
            <ListView x:Name="lst" CachingStrategy="RecycleElement" ItemsSource="{Binding PostsViewModels}" Grid.Row="1" SeparatorVisibility="None" ItemTemplate="{StaticResource PostTemplateSelector}" HasUnevenRows="true" BackgroundColor="#d3d3d3" ItemAppearing="Handle_ItemAppearing" IsPullToRefreshEnabled="true"
                IsRefreshing="{Binding IsPullRefreshLoading}" RefreshAllowed="true" RefreshCommand="{Binding OnRefreshPostsCommand}">
                <ListView.Header>
                    <Grid RowSpacing="0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid Grid.Row="0" BackgroundColor="White" HeightRequest="60" Margin="0,5">
                            <Grid.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding OnNewPostCommand}" />
                            </Grid.GestureRecognizers>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Controls:CustomImage Grid.Column="0" Margin="5" Source="{Binding Image.Path}" WidthRequest="50" HeightRequest="50" Style="{StaticResource ImageHeaderStyle}">
                                <Controls:CustomImage.Transformations>
                                    <fftransformations:CircleTransformation />
                                </Controls:CustomImage.Transformations>
                            </Controls:CustomImage>
                            <Label Grid.Column="1" FontSize="12" Text="{Binding WelcomeMessage}" TextColor="{StaticResource LightTextColor}" HorizontalOptions="Start" VerticalOptions="Center" />
                            <Grid Grid.Column="2" IsVisible="{Binding IsNotBusy}" Margin="15,0">
                                <Controls:ImageControl IsVisible="{Binding IsLoggedOut}" PortablePath="exit.png" Aspect="AspectFit" Style="{StaticResource IconStyle}">
                                    <Controls:CustomImage.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding OnLoginCommand}" />
                                    </Controls:CustomImage.GestureRecognizers>
                                </Controls:ImageControl>
                            </Grid>
                        </Grid>
                        <Grid x:Name="grdStartChallange" Margin="0,15,0,0" Grid.Row="1" RowSpacing="0" IsVisible="{Binding HasNotActiveChallenge}">
                            <Controls:CustomImage Aspect="AspectFill" Source="http://bit.ly/2gPXM5h" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand" />
                            <StackLayout Orientation="Vertical">
                                <Label Margin="0,5" Text="Start Your new challenge" FontSize="22" FontAttributes="Bold" HorizontalOptions="Center" TextColor="White" />
                                <Button Margin="0,0,0,5" Text="Start" Command="{Binding OnStartNewChallengeCommand}" WidthRequest="150" HorizontalOptions="Center" FontSize="14" Style="{StaticResource SecondaryButtonStyle}" />
                            </StackLayout>
                        </Grid>
                    </Grid>
                </ListView.Header>
            </ListView>
        </Grid>
    </ContentPage.Content>
</local:BasePage>